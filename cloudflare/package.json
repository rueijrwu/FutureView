{
  "name": "futureview-replay-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check worker/main.js && node --check worker/replay-session.js",
    "dry-run": "wrangler deploy --dry-run --outdir .dry-run"
  },
  "devDependencies": {
    "wrangler": "4.125.0"
  }
}
