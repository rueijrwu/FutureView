name: Strategy 1 QQQ Model OOS Diagnostics

on:
  push:
    branches:
      - cnn-trend-reset
    paths:
      - 'src/futureview/strategy1_success_model_oos_diagnostics.py'
      - 'src/futureview/strategy1_success_training.py'
      - 'src/futureview/strategy1_success_live_campaign_expanded_oos.py'
      - 'src/futureview/strategy1.py'
      - 'src/futureview/strategy1_reference_distribution.py'
      - 'src/futureview/strategy1_reference_distribution_fast.py'
      - 'src/futureview/features.py'
      - 'src/futureview/models.py'
      - 'src/futureview/data.py'
      - 'pyproject.toml'
      - '.github/workflows/strategy1-qqq-model-oos-diagnostics.yml'
  workflow_dispatch:

jobs:
  qqq-model-oos-diagnostics:
    runs-on: ubuntu-24.04
    env:
      FUTUREVIEW_TICKER: QQQ
      CUDA_VISIBLE_DEVICES: ''
    steps:
      - name: Checkout cnn-trend-reset
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - name: Install research dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install "torch>=2.4" "numpy>=1.26" "pandas>=2.2" "yfinance>=0.2.54" "numba>=0.61"
          python -m pip install -e . --no-deps

      - name: Diagnose QQQ model score quality
        run: |
          set -o pipefail
          futureview-strategy1-success-model-oos-diagnostics 2>&1 | tee strategy1-qqq-model-oos-diagnostics.log

      - name: Write results to job summary
        if: always()
        run: |
          {
            echo '## Strategy 1 QQQ Model OOS Diagnostics'
            echo
            echo 'Branch: `cnn-trend-reset`'
            echo 'Ticker: `QQQ`'
            echo 'Purpose: `model score quality, not trade frequency`'
            echo 'Selection threshold: `none`'
            echo 'Diagnostic: `within-fold score quintiles`'
            echo
            if [[ -f strategy1-qqq-model-oos-diagnostics.log ]]; then
              echo '```text'
              grep -E '^S1 MODEL_DIAGNOSTICS (DATA|RULE|FOLD|BUCKET|POOLED_BUCKET|SUMMARY|PASS)' strategy1-qqq-model-oos-diagnostics.log || true
              echo '```'
            else
              echo 'No model diagnostics log was produced.'
            fi
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload model diagnostics log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: strategy1-qqq-model-oos-diagnostics-${{ github.run_number }}
          path: strategy1-qqq-model-oos-diagnostics.log
          if-no-files-found: warn
          retention-days: 30
