services:
  replay:
    build: .
    ports:
      - "8787:8787"
    volumes:
      - ../data/databento/mes/raw:/data/raw:ro
      - ./runtime:/data/runtime
    command: ["serve", "--manifest", "/data/runtime/manifest.json", "--host", "0.0.0.0", "--port", "8787"]
